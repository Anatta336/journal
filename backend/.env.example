# Required: Password for the single user to log in.
# Set this to a strong password before starting the server.
AUTH_PASSWORD=changeme

# Optional: Number of days before bearer tokens expire. Default is 10.
TOKEN_EXPIRY_DAYS=10
